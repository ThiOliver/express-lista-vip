   <%-include('partials/head') %>
   <%-include('partials/header') %>

   <main>
     <h1><%= title %></h1>
     <p> <%= title %> | Só para convidados!</p>
     <% if(locals.convidados) { %>
      <ul>
        <% for(let convidado of convidados) { %>
          <li><%= convidado.nome %><%= convidado.vip ? ' [VIP]' : ' [Pipoca]' %></li>
          <% } %>
      </ul>
      <% } %>
   </main>

   <%-include('partials/footer') %>